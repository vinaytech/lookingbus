<%= form_for(@institution_app_page) do |f| %>
  <% if @institution_app_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@institution_app_page.errors.count, "error") %> prohibited this institution_app_page from being saved:</h2>

      <ul>
      <% @institution_app_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :institution_app_id %><br>
    <%= f.number_field :institution_app_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
